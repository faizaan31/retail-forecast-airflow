# Lighter dependency set for CI (no Airflow image build). Matches runtime libs used by ml_pipeline + API tests.
pandas>=2.1,<3
pyarrow>=14
statsforecast==1.7.7
joblib>=1.3
cloudpickle>=3
numpy>=1.26
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
pytest>=8.3
httpx>=0.27
ruff>=0.8
