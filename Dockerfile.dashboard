FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir "streamlit>=1.39" "pandas>=2.1,<3" "pyarrow>=14"

EXPOSE 8501
CMD ["streamlit", "run", "dashboard.py", "--server.address=0.0.0.0", "--server.port=8501"]
